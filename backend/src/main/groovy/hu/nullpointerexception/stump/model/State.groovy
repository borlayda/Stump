package groovy.hu.nullpointerexception.stump.model

class Status {

    enum Task {
        OPEN,
        CLOSED
    }

    enum Project {
        OPEN,
        CLOSED
    }

    enum User {
        ACTIVE,
        INACTIVE
    }

}