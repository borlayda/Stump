package hu.nullpointerexception.stump.model

class State {

    enum Task {
        OPEN,
        CLOSED
    }

    enum Project {
        OPEN,
        CLOSED
    }

    enum User {
        ACTIVE,
        INACTIVE
    }

}